#include <cstdio>
#include <stack>
using namespace std;

void dfs(int number){
    bool visited[8] = {};
    visited[number] = true;
    stack<int> s;
    s.push(number);
    while(!s.empty()){
        int 
    }
}

int main(){
    int N;
    scanf("%d", &N);
    
    return 0;
}
